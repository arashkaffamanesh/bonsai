apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-test
  namespace: cattle-system 
spec:
  rules:
  - host: 192.168.64.19.xip.io
    http:
      paths:
      - path: /
        backend:
          serviceName: rancher 
          servicePort: 32677 
